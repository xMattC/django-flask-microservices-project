name: Checks

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  placeholder-check:
    name: Placeholder Check
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Initial CI scaffold in place. Docker-based checks will be added later."